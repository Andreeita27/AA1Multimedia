<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:fitsSystemWindows="true"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvProfessionalsTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/menu_professionals"
        android:textColor="#b5a16c"
        android:textStyle="bold"
        android:textSize="22sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnAddProfessional"
        app:layout_constraintHorizontal_bias="0" />

    <ImageButton
        android:id="@+id/btnAddProfessional"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="@drawable/bg_icon_button"
        android:contentDescription="@string/menu_add_professional"
        android:src="@android:drawable/ic_input_add"
        app:tint="#b5a16c"
        app:layout_constraintTop_toTopOf="@id/tvProfessionalsTitle"
        app:layout_constraintBottom_toBottomOf="@id/tvProfessionalsTitle"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvProfessionalsSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/professional_subtitle"
        android:textColor="#ffffff"
        android:alpha="0.85"
        android:textSize="14sp"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@id/tvProfessionalsTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProfessionals"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvProfessionalsSubtitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>